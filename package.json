{
  "name": "ec-qa-xray-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "The qa-xray agent and the tooling it uses to turn Jira specs into Xray test cases.",
  "type": "module",
  "scripts": {
    "test": "node --test .claude/scripts/test/*.test.mjs"
  },
  "license": "Apache-2.0"
}
