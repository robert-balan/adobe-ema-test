{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QA plan file",
  "description": "Reviewable intermediate artifact produced by the qa-xray agent and consumed by .claude/scripts/xray-push.mjs.",
  "type": "object",
  "required": [
    "project",
    "tests"
  ],
  "additionalProperties": false,
  "properties": {
    "project": {
      "type": "string",
      "description": "Jira project key the Tests are created in, e.g. \"EC\"."
    },
    "source": {
      "type": "object",
      "description": "The ticket these tests were derived from.",
      "additionalProperties": false,
      "properties": {
        "key": {
          "type": "string"
        },
        "summary": {
          "type": "string",
          "description": "The ticket's summary when the plan was written. spec-drift.mjs compares it against the live one to catch a repurposed ticket."
        },
        "url": {
          "type": "string"
        },
        "acDigest": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{16}$",
          "description": "Digest of the ticket's acceptance-criteria section when the plan was written, written by spec-drift.mjs --record. Catches the common case a summary comparison cannot: same title, rewritten criteria."
        },
        "acCheckedOn": {
          "type": "string",
          "description": "ISO date the digest was recorded, so an unreviewed plan is visible as such."
        }
      },
      "required": [
        "key"
      ]
    },
    "folder": {
      "type": "string",
      "description": "Default Xray Test Repository folder path, e.g. \"/Header/Megamenu\"."
    },
    "previewBase": {
      "type": "string",
      "description": "Origin the fixture paths are opened from, e.g. https://develop--ufs--foodsolutions-04.aem.page. Branch-specific by nature, so it is stated once here rather than baked into a step \u2014 the Test Execution's environment records which branch a result belongs to."
    },
    "testSets": {
      "type": "object",
      "description": "Optional override of the Test Set summary per suite. Omit it \u2014 the default is three project-wide sets (\"Sanity testing\", \"Regression testing\", \"E2E testing\") that accumulate tests across every ticket. Naming them here creates ticket-scoped sets instead, since the registry is keyed by summary.",
      "properties": {
        "sanity": {
          "type": "string"
        },
        "regression": {
          "type": "string"
        },
        "e2e": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "fixtures": {
      "type": "array",
      "description": "Authored test content in Document Authoring, generated rather than hand-built. A step cites a fixture instead of describing the authoring work, which is what makes a small number of broad tests workable.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "title",
          "page"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9]*-FX-[0-9]{2,}$",
            "description": "Stable identity, e.g. BRANDS-FX-01. Never renumber one \u2014 tests cite it."
          },
          "title": {
            "type": "string"
          },
          "purpose": {
            "type": "string",
            "description": "What this fixture exists to prove, in one line."
          },
          "page": {
            "type": "string",
            "description": "Path of the fixture page in DA, e.g. /drafts/qa/brands/happy. Never under a published path."
          },
          "nav": {
            "type": "object",
            "description": "A nav document generated for this fixture by transforming a live one. Header-family blocks live in the site-wide nav, so a variant means a whole nav rather than a section; the page's `nav` metadata row is derived from `path` automatically. A spec may edit more than one region: `utility` for the top bar, `logo` and `menus` for the nav row itself, and the count/label/links keys for the brand strip inside the Products megamenu.",
            "additionalProperties": false,
            "required": [
              "path",
              "from"
            ],
            "properties": {
              "path": {
                "type": "string",
                "description": "Where the generated nav is written, under /drafts/."
              },
              "from": {
                "type": "string",
                "description": "The live document it is derived from, e.g. /nav. Deriving keeps the fixture identical to production except on the axis under test."
              },
              "count": {
                "type": "integer",
                "description": "How many brand links the strip should hold. Logos cycle from the source when more are wanted than exist."
              },
              "label": {
                "type": "string",
                "description": "Replacement text for the strip label."
              },
              "removeLabel": {
                "type": "boolean",
                "description": "Drop the label entirely, for the degraded case."
              },
              "links": {
                "type": "array",
                "description": "Per-link overrides, positional: { alt, src, caption }.",
                "items": {
                  "type": "object"
                }
              },
              "tools": {
                "type": "array",
                "description": "Replace the nav's tools row \u2014 Search, Favorites, Cart, Account. These live in the nav's LAST list, not its first, which is why `utility` does not reach them; an Arabic fixture needs them or the header keeps an English tools row. Entries keep their icon token: \":search: \u0628\u062d\u062b|/search\".",
                "items": { "type": "string" }
              },
              "utility": {
                "type": "object",
                "description": "Edits to the nav's utility bar \u2014 its first section, holding the author-defined links and the language block.",
                "additionalProperties": false,
                "properties": {
                  "links": {
                    "type": "array",
                    "description": "Utility links as \"Label|/href\", replacing the authored list.",
                    "items": {
                      "type": "string"
                    }
                  },
                  "trigger": {
                    "type": "string",
                    "description": "The language switcher's trigger label, e.g. \":globe: United States \u00b7 EN\"."
                  },
                  "languages": {
                    "type": "array",
                    "description": "Language options as \"Label \u00b7 CODE|/href\". One entry is the single-language market case.",
                    "items": {
                      "type": "string"
                    }
                  },
                  "note": {
                    "type": "string",
                    "description": "The country note shown under the language list."
                  },
                  "change": {
                    "type": "string",
                    "description": "The \"Change country\" link, as \"Label|/href\"."
                  }
                }
              },
              "logo": {
                "type": "object",
                "description": "Edits to the nav's logo. The Content Slots table gives it one fallback \u2014 text brand when there is no image \u2014 and an author produces that by typing the brand name where the icon token was.",
                "additionalProperties": false,
                "properties": {
                  "text": {
                    "type": "string",
                    "description": "Replaces the logo icon with this text, keeping the link around it."
                  }
                }
              },
              "menus": {
                "type": "object",
                "description": "Edits to the nav's top-level items. Each item is one div.nav-menu block: row 1 is the link shown on the bar, rows 2+ are its megamenu columns. Items are built by cycling the live blocks, so a fixture with six items still carries real megamenu content.",
                "additionalProperties": false,
                "required": [
                  "items"
                ],
                "properties": {
                  "items": {
                    "type": "array",
                    "description": "The top-level items, in bar order.",
                    "items": {
                      "type": "object",
                      "additionalProperties": false,
                      "properties": {
                        "label": {
                          "type": "string",
                          "description": "The label shown on the bar."
                        },
                        "href": {
                          "type": "string",
                          "description": "Where the bar link points."
                        },
                        "plain": {
                          "type": "boolean",
                          "description": "Drop the megamenu columns, leaving a link with nothing to open. That absence is what makes an item plain \u2014 there is no flag in the content model."
                        },
                        "columnsExact": {
                          "type": "boolean",
                          "description": "Treat `columns` as the panel's complete set: live columns past the end of the list are dropped rather than left in place. Needed when the point of the fixture IS the column count \u2014 a one-column panel cannot be authored by replacing the first of three. Off by default, because leaving unreached columns alone is what a translation fixture wants and blanking them silently is the worse mistake."
                        },
                        "columns": {
                          "type": "array",
                          "description": "Replace this item's labelled megamenu columns, positionally by kind \u2014 tiles and brand strips are left where they are, and live columns the list does not reach are left alone. Needed for an RTL or text-expansion fixture, where translating only the bar label leaves every submenu link in the source language.",
                          "items": {
                            "type": "object",
                            "additionalProperties": false,
                            "properties": {
                              "heading": {
                                "type": "string",
                                "description": "The column heading. Authored as a plain paragraph, never bold \u2014 a <strong> here would reclassify the row as a promotional tile."
                              },
                              "links": {
                                "type": "array",
                                "description": "The column's links as \"Label|/href\".",
                                "items": { "type": "string" }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              },
              "promo": {
                "type": "array",
                "description": "Edits to a megamenu's promotional tile. The tile has no block and no class of its own: it is a submenu row whose first cell holds loose paragraphs \u2014 eyebrow, **title**, an optional description, then the CTA \u2014 and the implementation reads meaning out of that order. Which variant it becomes is decided by POSITION, not by the author: the panel's first child is the light `.nav-feature`, any later child the dark `.nav-promo`. One entry per tile edited.",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "menu": {
                      "type": "string",
                      "description": "Which megamenu to edit, named as it reads on the bar (e.g. \"Products\"). Omit to take the first menu that already has a tile."
                    },
                    "target": {
                      "type": "string",
                      "enum": ["feature", "promo"],
                      "description": "Which existing tile to edit when a panel holds both \u2014 Recipes ships a leading feature and a trailing promo. Defaults to the first."
                    },
                    "position": {
                      "type": "string",
                      "enum": ["first", "last"],
                      "description": "Where the tile is authored among the panel's rows, and so which variant it becomes: \"first\" is the light feature, \"last\" a dark promo after the columns. Omit to edit the tile where it already sits, so a copy change does not silently change the variant too."
                    },
                    "remove": {
                      "type": "boolean",
                      "description": "Drop the tile entirely, for the panel-with-no-promo case."
                    },
                    "eyebrow": { "type": "string", "description": "The overline label, authored as the first paragraph." },
                    "title": { "type": "string", "description": "The title, authored bold \u2014 which is how the implementation finds it." },
                    "strongTitle": {
                      "type": "boolean",
                      "description": "Set false for the author who forgot the bold. The classifier then falls back to the second paragraph."
                    },
                    "description": {
                      "type": "string",
                      "description": "The body paragraph. The content model gives it to the feature only; authoring one on a promo is a real case and lands on .nav-promo-desc, which has no styling of its own."
                    },
                    "cta": { "type": "string", "description": "The call to action, as \"Label|/href\"." },
                    "extraCta": {
                      "type": "string",
                      "description": "A second CTA, as \"Label|/href\". The content model forbids it and nothing enforces that, so a fixture can prove what happens."
                    },
                    "ctaIcon": {
                      "type": "boolean",
                      "description": "Author a trailing :arrow: icon token on the CTA. The stylesheet sizes an authored icon; it no longer draws an arrow of its own."
                    },
                    "ctaInSecondCell": {
                      "type": "boolean",
                      "description": "Author the CTA in the row's second cell. Links there with no bold title reclassify the whole row as a labelled column \u2014 the mis-authoring that costs the tile its card."
                    }
                  }
                }
              }
            }
          },
          "footer": {
            "type": "object",
            "description": "A footer document generated for this fixture by transforming the live one. `footer.js` reads getMetadata('footer') and falls back to /footer, so the page's `footer` metadata row is derived from `path` automatically \u2014 the same mechanism as `nav`, a different key. Deriving rather than authoring keeps the fixture identical to production except on the axis under test.",
            "additionalProperties": false,
            "required": [
              "path",
              "from"
            ],
            "properties": {
              "path": {
                "type": "string",
                "description": "Where the generated footer document is written, under /drafts/."
              },
              "from": {
                "type": "string",
                "description": "The live document it is derived from, e.g. /footer."
              },
              "newsletter": {
                "type": "object",
                "description": "Edits to the band \u2014 section one, the standalone `newsletter` block. Its cell holds a heading, body copy, and the link that becomes the Subscribe form.",
                "additionalProperties": false,
                "properties": {
                  "remove": {
                    "type": "boolean",
                    "description": "Drop the whole band section, for the footer-with-no-newsletter case."
                  },
                  "heading": { "type": "string", "description": "Replacement band title." },
                  "headingLevel": {
                    "type": "integer",
                    "enum": [1, 2, 3, 4, 5, 6],
                    "description": "The level the heading is authored at. decorate() promotes whatever it finds to an h2 so the footer does not skip a level after the page's h1; authoring an h2 directly proves the already-correct path."
                  },
                  "removeHeading": { "type": "boolean", "description": "Drop the heading entirely." },
                  "subtext": { "type": "string", "description": "Replacement body copy." },
                  "removeSubtext": { "type": "boolean", "description": "Drop the optional body copy." },
                  "cta": {
                    "type": "string",
                    "description": "The call to action, as \"Label|/href\". The href becomes the form action; the label becomes the submit button."
                  },
                  "removeCta": {
                    "type": "boolean",
                    "description": "Drop the link, so no form is built at all \u2014 the required-slot-absent case."
                  }
                }
              },
              "brand": {
                "type": "object",
                "description": "Edits to the brand column \u2014 the leading group of paragraphs in section two, before the first bold heading.",
                "additionalProperties": false,
                "properties": {
                  "logoText": {
                    "type": "string",
                    "description": "Replace the :ufs-logo: icon token with this text, keeping the link around it. This is how an author produces the Content Slots table's text-brand fallback."
                  },
                  "removeLogo": { "type": "boolean", "description": "Drop the logo paragraph entirely." },
                  "tagline": {
                    "type": "string",
                    "description": "The tagline, as \"lead|accent\". decorate() wraps whatever follows the <br> in the script accent, so omitting the | authors a tagline with no accent."
                  },
                  "removeTagline": { "type": "boolean", "description": "Drop the tagline." }
                }
              },
              "columns": {
                "type": "array",
                "description": "Replace the link columns. There are no column divs in the document \u2014 DA flattens them and decorate() starts a new column at every <p> holding a <strong> and no image. An empty array removes every link column.",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "heading": { "type": "string", "description": "The column heading." },
                    "strongHeading": {
                      "type": "boolean",
                      "description": "Set false for the author who forgot the bold. Nothing then starts a new column, so this column silently merges into the previous one."
                    },
                    "links": {
                      "type": "array",
                      "description": "The column's links as \"Label|/href\".",
                      "items": { "type": "string" }
                    }
                  }
                }
              },
              "bottom": {
                "type": "object",
                "description": "Edits to the bottom bar \u2014 section three: a copyright paragraph and a paragraph of social icon links.",
                "additionalProperties": false,
                "properties": {
                  "remove": { "type": "boolean", "description": "Drop the whole bottom bar section." },
                  "copyright": { "type": "string", "description": "Replacement copyright line." },
                  "removeCopyright": { "type": "boolean", "description": "Drop the copyright." },
                  "socials": {
                    "type": "array",
                    "description": "Social accounts as \"Label|https://host/\", optionally \"Label|https://host/|icon-token\". The icon token defaults to the label lowercased. decorate() names each icon-only link from its hostname, so the label and the host are what a test compares.",
                    "items": { "type": "string" }
                  },
                  "removeSocials": { "type": "boolean", "description": "Drop the social row, for the bottom-bar-with-no-socials case." }
                }
              }
            }
          },
          "metadata": {
            "type": "object",
            "description": "Page metadata rows, e.g. { \"nav\": \"/drafts/qa/brands/nav-happy\", \"Theme\": \"light\" }."
          },
          "sections": {
            "type": "array",
            "description": "Sections on the page, in order. Omit for a fixture whose content is entirely in its nav document.",
            "items": {
              "type": "object"
            }
          }
        }
      }
    },
    "tests": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "summary",
          "ac",
          "suites",
          "steps"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9]*(-[0-9]+)?-TC-[0-9]{2,}$",
            "description": "Stable identity, and the idempotency key for pushes \u2014 NEVER renumber one. Prefer a feature slug (BRANDS-TC-01) over a ticket key (EC-14-TC-01): spec tickets get repurposed and renumbered, features do not."
          },
          "summary": {
            "type": "string",
            "description": "Jira summary. The format is defined once, in the Conventions section of .claude/agents/qa-xray.md \u2014 do not restate it here, because two statements of it drift apart."
          },
          "ac": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Acceptance criteria this test traces to. May be empty ONLY for a standing requirement the agent must cover whether or not the ticket mentions it (WCAG 2.1 AA, RTL) \u2014 and such a test must then explain itself in `notes` and be listed under Coverage gaps. xray-push enforces that pairing; an empty list with no explanation is rejected."
          },
          "suites": {
            "type": "array",
            "minItems": 1,
            "items": {
              "enum": [
                "sanity",
                "regression",
                "e2e"
              ]
            }
          },
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Extra dimension labels; whitespace is hyphenated on push."
          },
          "scope": {
            "type": "string",
            "description": "What this test covers, in plain English, for a reader who has not read the ticket. Published into the Jira description."
          },
          "fixtures": {
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^[A-Z][A-Z0-9]*-FX-[0-9]{2,}$"
            },
            "description": "Fixture ids this test runs against. Their URLs are published into the Jira description so a tester never has to author anything."
          },
          "precondition": {
            "type": "string",
            "description": "Environment state required before step 1, beyond opening a fixture."
          },
          "notes": {
            "type": "string"
          },
          "folder": {
            "type": "string",
            "description": "Overrides plan.folder for this test."
          },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": [
                "action",
                "result"
              ],
              "additionalProperties": false,
              "properties": {
                "action": {
                  "type": "string",
                  "description": "What the tester does. A step that sends them to a fixture page must also name that fixture in `fixtures`, which is what puts the full preview URL into the step's data — prose alone leaves them hunting for the page."
                },
                "fixtures": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Fixture ids this step opens, in the order the tester opens them. Each is rendered as `{previewBase}{fixture.page}` at the top of the step's Test Data, so the tester gets a URL to paste rather than an id to look up. Set it on every step that navigates — the first step of a test, a step that moves to a second fixture, a step that compares one against another — and leave it off a step that stays on the page already open, such as resizing, hovering or inspecting. Ids only, never a written-out URL: the path lives once in the plan's `fixtures`, and a second copy is a second thing to leave behind when a page moves. Every id must also appear in the test's own `fixtures`."
                },
                "data": {
                  "type": "string",
                  "description": "Test Data: the values this step needs — a viewport, an input, an expected colour. The URLs from `fixtures` are prepended to it, so never write a page path or an origin here."
                },
                "result": {
                  "type": "string",
                  "description": "What the tester should see. One observable outcome, stated so that it can fail."
                }
              }
            }
          }
        }
      }
    },
    "projectId": {
      "type": "string",
      "description": "Numeric Jira project id, required only to pre-create the Test Repository folder (EC = 11590)."
    },
    "assignee": {
      "type": "string",
      "description": "Atlassian accountId to assign every created Test to. Omit to leave unassigned."
    },
    "feature": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]*$",
      "description": "Feature slug these tests belong to; matches the prefix of every test id."
    }
  }
}
